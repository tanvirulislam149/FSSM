import React from "react";
import style from "./Header.module.css";

const Header = () => {
   return (
      <div style={{ paddingTop: "5px" }}>
         <p className={style.header}>Get Involved</p>
      </div>
   );
};

export default Header;
